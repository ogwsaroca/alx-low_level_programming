#include "main.h"

/**
 * nul - Mulitiplies two integers
 * @x: The first number to multiply
 * @y: The second number to multiply with second number
 * Return:The result of multiplication between x param and y param
 */

int nul(int x, int y)
{
	return (x * y);
}
